name: Test build & release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

env:
  ARTIFACT_PREFIX: tt-rss

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - uses: cachix/install-nix-action@v17
      with:
        # nix_path: nixpkgs=channel:nixos-unstable
        nix_path: nixpkgs=https://github.com/nixos/nixpkgs/archive/staging-22.11.tar.gz

    # build without systemd support for now, since the closure becomes larger
    - run: nix-build --arg withSystemd false

    - name: Prepare artifacts
      run: |
        cp result/${{ env.ARTIFACT_PREFIX}}_*.raw ./
        sh -c 'sha256sum $0 > $0.sha256' ${{ env.ARTIFACT_PREFIX }}_*.raw

    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        if-no-files-found: error
        retention-days: 5
        path: |
          ${{ env.ARTIFACT_PREFIX }}_*.raw
          ${{ env.ARTIFACT_PREFIX }}_*.raw.sha256
        name: ${{ env.ARTIFACT_PREFIX }}

    - name: Create a github release from tag
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        prerelease: true
        files: |
          ${{ env.ARTIFACT_PREFIX }}_*.raw
          ${{ env.ARTIFACT_PREFIX }}_*.raw.sha256
